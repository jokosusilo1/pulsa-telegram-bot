services:
  # REST API Server
  - type: web
    name: pulsa-api
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        fromDatabase:
          name: pulsa-db
          property: connectionString
      - key: DIGIFLAZZ_USERNAME
        sync: false
      - key: DIGIFLAZZ_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true

  # Telegram Bot (Background Worker)
  - type: worker
    name: pulsa-bot
    env: node
    plan: free
    rootDir: bot
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: BOT_TOKEN
        sync: false
      - key: API_BASE_URL
        fromService:
          name: pulsa-api
          type: web
          property: host

# MongoDB Database (Jika pakai MongoDB di Render - optional)
databases:
  - name: pulsa-db
    plan: free
    databaseName: pulsa
    user: pulsa-admin